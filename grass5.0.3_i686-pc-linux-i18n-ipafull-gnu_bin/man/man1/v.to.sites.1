v.to.sites(1)                 Grass User's Manual                v.to.sites(1)



NNAAMMEE
       vv..ttoo..ssiitteess   -  Converts  point data in a binary GRASS vector map layer
       into a GRASS _s_i_t_e___l_i_s_t_s file.
       _(_G_R_A_S_S _V_e_c_t_o_r _P_r_o_g_r_a_m_)

SSYYNNOOPPSSIISS
       vv..ttoo..ssiitteess
       vv..ttoo..ssiitteess hheellpp
       vv..ttoo..ssiitteess [[--aaccCCiidd] iinnppuutt==_n_a_m_e oouuttppuutt==_n_a_m_e [ddmmaaxx==_v_a_l_u_e]

DDEESSCCRRIIPPTTIIOONN
       The _v_._t_o_._s_i_t_e_s program extracts data from a GRASS vector map layer  and
       stores output in a new GRASS _s_i_t_e___l_i_s_t_s file.  If  --aa flag is selected,
       _v_._t_o_._s_i_t_e_s extracts all vertices from a vector file, if  not  selected,
       it  extracts  site (point) features only, ignoring lines and areas.  If
       --ii flag is selected then, for each line, if the  distance  between  any
       two  vertices  on this line is greater that ddmmaaxx, additional points are
       added to keep the distance withing ddmmaaxx range.  The resulting sites map
       layer can be used with such programs as _d_._s_i_t_e_s.


       The  user can run the program non-interactively by specifying the names
       of an existing vector iinnppuutt map layer and a new site list  file  to  be
       oouuttppuutt  on  the command line.  The program will be run interactively if
       the user types vv..ttoo..ssiitteess without arguments on the  command  line.   In
       this  case, the user will be prompted to enter parameter values through
       the standard user interface described in the manual entry for _p_a_r_s_e_r.

OOPPTTIIOONNSS::
   FFllaaggss::
       --aa     Outputs all vertices (instead of site  data  only)  from  vector
              file to site file

       --cc     The Category NUMERIC data is used instead of attribute as a site
              description

       --CC     The Category TEXT data is used instead of attribute  as  a  site
              description

       --ii     Additional  sites  are  added between each 2 points on a line if
              the distance  between  them  is  greater  than  specified  ddmmaaxx.
              (valid only when --aa is used)

       --dd     Write attribute as double instead of cat.


       If  any  of  the sites have been labeled in _v_._d_i_g_i_t, then the resultant
       site list will contain category information (or attribute  in  case  --aa
       was  used  but --cc was not).  If none of the sites are labeled, a binary
       (0/1) site list file will be produced.


   PPaarraammeetteerrss::
       iinnppuutt==_n_a_m_e
              Name of an existing binary vector map layer from which site data
              are to be extracted.

       oouuttppuutt==_n_a_m_e
              Name to be assigned to the resultant _s_i_t_e___l_i_s_t_s file.

       ddmmaaxx==_v_a_l_u_e
              Maximum  distance  between points (valid only when --aa and --ii are
              used)


NNOOTTEESS
       Vector lines must be labeled.


       If the flag --ii is selected, a spline function is used to generate addi-
       tional points along the line. To avoid the generating of lots of points
       along contours, ddmmaaxx== should be really large so  that  vv..ttoo..ssiitteess  does
       not  add  any  addtional sites to the data. On the other hand, if addi-
       tional sites data are desired, ddmmaaxx== should be smaller  than  the  dis-
       tances between existing data points.

SSEEEE AALLSSOO
       _d_._s_i_t_e_s, _v_._d_i_g_i_t and _p_a_r_s_e_r

AAUUTTHHOORR
       Dave Gerdes,
       Irina Kosinovsky,
       U.S.Army Construction Engineering Research Laboratory

       _L_a_s_t _c_h_a_n_g_e_d_: _$_D_a_t_e_: _2_0_0_2_/_0_1_/_2_5 _0_5_:_4_5_:_3_5 _$



GRASS 5.0                                                        v.to.sites(1)
